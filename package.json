{
  "name": "removal-ia",
  "private": true,
  "engines": {
    "node": "22.x"
  },
  "scripts": {
    "build": "cd client && npm install && npm run build",
    "start": "cd server && node server.js"
  },
  "dependencies": {
    "express": "^4.21.1",
    "cors": "^2.8.5",
    "dotenv": "^16.4.7"
  }
} 